#!/usr/bin/python

def calls_comparison():
    print('bolo tarara')
    #open both the json files
    checker_file = open('checker.json', 'r')
    target_file = open('target_dump.json', 'r')
    #close both the file
    checker_file.close()
    target_file.close()
